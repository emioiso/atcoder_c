#include <stdio.h>

int	main(void)
{
	int a;
	int b;
	int k;

	scanf("%d%d%d",&a,&b,&k);
	// printf("%d %d %d\n",a,b,k);

	if(b < k){

		int i;
		for (i = a; i <= b; i++)
		{
			printf("%d\n", i);
		}
	} else if(){

	}
	return (0);
}
